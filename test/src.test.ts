import { getHistory, getBookmark } from '../src';

getHistory('bilibili');
// .then(console.log);
getBookmark();
// .then(console.log);
it('should add two numbers', () => {
  return;
  expect(3).toMatchSnapshot();
});
